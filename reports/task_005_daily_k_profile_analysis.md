# TASK_005 日K数据画像诊断报告

## 一、基础规模

- 总行数：16,548,275
- 股票数量：5,523
- 最早交易日期：1990-12-19T00:00:00
- 最晚交易日期：2026-05-29T00:00:00
- 主键重复额外行数：0
- OHLC逻辑异常数：0

## 二、已通过的结构质量检查

以下项目已通过：

- `stock_code` 无空值
- `trade_date` 无空值
- OHLC 无空值
- OHLC 均为正数
- `high >= low`
- `high >= open/close`
- `low <= open/close`
- `stock_code + trade_date` 无重复

## 三、price_change 诊断

可比较行数：16,542,752

- 不匹配行数：820
- 不匹配率：0.004957%

结论：

`price_change` 与 `close - prev_close` 在绝大多数记录中一致。
820条不匹配记录需要单独抽样，重点排查除权除息、停牌恢复、
历史数据修订或异常记录。

## 四、pct_change 诊断

- 常规公式不匹配数：16,094,398
- 常规公式不匹配率：97.2897%
- 反向分母公式不匹配数：10,468,080
- 反向分母公式不匹配率：63.2790%

上传报告中的20条异常样例全部满足：

`pct_change = round((prev_close - close) / prev_close * 100, 2)`

即常规涨跌幅公式取反。

当前还需要执行一次全表统计，确认该公式在全表的匹配数量。

## 五、空值模式

- `pct_change` 空值数：5,523
- `price_change` 空值数：5,523

两者都等于股票数量 5,523，高度符合“每只股票首个交易日
缺少前收盘价”的模式，但仍应通过分组统计确认。

周期收益字段和BOLL字段的空值数量呈现明显的窗口预热特征。

公司行为字段非空记录数约为：53,133。
五个公司行为字段空值数量完全相同，需要确认这些字段是在公司行为日
统一填充，还是上游做过统一补值。

## 六、单位与复权结论

当前仍不能确认：

- `amount` 单位
- `volume` 单位
- `float_shares` / `total_shares` 单位
- `open/high/low/close` 的复权口径
- `adj_factor` 与 `adj_price` 的确切计算定义

现有复权统计与此前5行样例存在矛盾：
此前样例中 `adj_factor = 1` 且 `adj_price = close`，
但本次汇总却显示所有行均不等于1且均与close不同。

因此本次JSON中的复权汇总不能直接采信，需要修正统计表达式后重跑。

## 七、当前验收状态

TASK_005 的“画像读取与结构质量检查”已通过。

TASK_005 的“字段语义验收”仍未通过，原因包括：

- `pct_change` 公式需要全表确认
- 复权字段统计需要修正
- 量额与股本单位未确认
- 最新数据日期停在2026-05-29，需要确认更新链路

当前状态应保持：

- `overall_status = PENDING_CONFIRMATION`
- `blocks_downstream = true`

在上述问题解决前，不应直接进入正式因子计算和回测。

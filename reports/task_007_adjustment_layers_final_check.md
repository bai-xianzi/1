# TASK_007 复权字段分层核验最终报告

## 一、验收结论

TASK_007 已完成。

本任务对 DolphinDB 日K表中的复权字段进行了只读、分层、分批核验。

- 数据库：`dfs://A_STOCK_DAILY_K_DB`
- 数据表：`stock_daily_k`
- 总行数：16,548,275
- 股票数量：5,523
- 分层核验批次数：56
- 最新交易日期：2026-05-29
- 日历滞后：24天
- 整体状态：`PENDING_CONFIRMATION`
- 阻断下游：`True`

阻断原因只剩数据时效性，复权公式本身已经得到高置信度确认。

## 二、复权公式结论

四个分层的最佳公式覆盖率均为100%。

### 2.1 deduct_value 等于零

无论公司行为字段是否非零，都满足：

`adj_price = close × adj_factor`

### 2.2 deduct_value 非零

无论公司行为字段是否非零，都满足：

`adj_price = close × adj_factor + deduct_value`

### 2.3 统一公式

可统一写成：

`adj_price = close × adj_factor + deduct_value`

当 `deduct_value = 0` 时，该公式自然退化为：

`adj_price = close × adj_factor`

## 三、覆盖范围

可比较记录数：

16,548,214

全表记录数：

16,548,275

覆盖比例：

99.999631%

未进入公式比较的记录：

61

这61行占全表约0.000369%，应在后续标准化层作为边界记录单独检查。
当前报告只能确认它们未满足比较条件，不能直接判断其原因。

## 四、复权因子变化

复权因子变化记录数：

12,959

其中：

- 公司行为字段非零：12,959，占100%
- deduct_value 非零：12,397，占约95.6632%
- deduct_value 等于零：562

说明复权因子的变化与公司行为高度一致。

`deduct_value` 并不是每次因子变化都必须非零，因此应把它视为复权公式中的独立修正项，而不是公司行为发生与否的唯一标志。

## 五、字段语义建议

建议正式固化以下来源字段语义：

- `close`：来源日K收盘价，具体属于未复权价还是其他价格口径仍需结合来源说明固化名称
- `adj_factor`：累计复权乘数
- `deduct_value`：复权价加法修正项
- `adj_price`：按以下公式得到的来源复权价

`adj_price = close × adj_factor + deduct_value`

项目标准化层应：

1. 保留全部来源原字段；
2. 不覆盖 DolphinDB 原始数据；
3. 记录统一公式及验证版本；
4. 对61条边界记录增加质量标记；
5. 后续因子计算明确使用 `close` 还是 `adj_price`。

## 六、数据时效性

最新数据日期为：

2026-05-29

核验运行日期相对该日期滞后24个日历日。

因此，复权字段语义已通过，但当前数据库仍不能被认定为最新行情库。

需要进一步检查：

- 2026-05-29之后是否存在源文件；
- 导入程序是否停止；
- 自动更新任务是否未配置；
- 数据供应端是否尚未更新；
- 最近一批文件是否导入失败。

## 七、最终状态

复权字段语义：

`PASSED`

数据更新状态：

`PENDING_CONFIRMATION`

整个数据源：

- `overall_status = PENDING_CONFIRMATION`
- `blocks_downstream = true`

在完成数据更新链路检查前，不进入依赖最新行情的正式实盘选股流程。

## 八、下一任务

`TASK_008：数据更新链路与最新日期核验`

目标：

1. 检查本地源文件最新日期；
2. 检查导入成功和失败日志；
3. 对比源文件、DolphinDB和系统日期；
4. 定位数据停在2026-05-29的原因；
5. 输出安全的增量更新方案。
